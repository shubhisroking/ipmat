module.exports = {
  extends: ['expo', 'prettier'],
  plugins: ['prettier'],
  rules: {
    'prettier/prettier': 'error',
  },
};
// This ESLint configuration extends the Expo and Prettier configurations.
// It also includes the Prettier plugin and enforces Prettier rules as errors.
